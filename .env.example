# This is an example environment file.
# For your deployment, copy this file to .env and fill in your actual secret values.
# The .env file is ignored by Git for security.

# Generate a strong, random key for the Master Key.
# Command to generate a new key: openssl rand -hex 16
MASTER_KEY=your_super_secret_master_key_please_change
